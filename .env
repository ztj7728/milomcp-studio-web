# ===================================================================
# MiloMCP Studio - Environment Variables Example
# ===================================================================
#
# 1. Copy this file to a new file named .env in the project root.
#    cp .env.example .env
# 2. Fill in the necessary values for your local development environment.
# 3. This file should be committed to version control.
#    The .env file (with your secrets) should NOT be committed.
#
# ===================================================================

# Application Settings
# Set to 'development' for local development.
# The Dockerfile will override this to 'production' for production builds.
NODE_ENV=development
PORT=3000

# Next.js & NextAuth Configuration
# For local development, this should match your application's URL.
NEXTAUTH_URL=http://localhost:3000
# A secret key for signing JWTs. Generate a strong one with:
# openssl rand -hex 32
NEXTAUTH_SECRET=0344af54b85b6870343c906ac8251569263bb5f6b5793c14d181e9e08ee7d6bc

# API Configuration
# URL of the backend API that the studio will connect to.
NEXT_PUBLIC_API_URL=http://localhost:3030
NEXT_PUBLIC_WS_URL=ws://localhost:3030/ws
# A display URL for the API, shown in the UI.
NEXT_PUBLIC_API_URL_DISPLAY=http://localhost:3030

# ===================================================================
# Optional Services (uncomment and configure if needed)
# ===================================================================

# Database (if using external database in the future)
# DATABASE_URL=postgresql://user:password@localhost:5432/milomcp

# Redis (if using for sessions/cache)
# REDIS_URL=redis://localhost:6379

# Email Configuration (if adding email features)
# EMAIL_SERVER_HOST=smtp.gmail.com
# EMAIL_SERVER_PORT=587
# EMAIL_SERVER_USER=your-email@gmail.com
# EMAIL_SERVER_PASSWORD=your-app-password
# EMAIL_FROM=noreply@yourdomain.com

# OAuth Providers (if adding social login)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=